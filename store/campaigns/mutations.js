export default {
  /**
   * Set the campaigns
   * @param state
   * @param campaigns
   */
  setCampaigns(state, campaigns) {
    state.campaigns = campaigns;
  },
  /**
   * Set the state
   * @param state
   * @param s
   */
  setState(state, s) {
    state.state = s;
  },
};

export default {
  /**
   * Get all campaigns
   * @param state
   * @return {[]}
   */
  getCampaigns: state => state.campaigns,
  /**
   * Get the campaigns state
   * @param state
   * @return {*}
   */
  getState: state => state.state,
};

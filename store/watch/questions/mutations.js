export default {
  /**
   * Set the video
   * @param state
   * @param questions
   */
  setQuestions(state, questions) {
    state.questions = questions;
  },
  /**
   * Set the state
   * @param state
   * @param s
   */
  setState(state, s) {
    state.state = s;
  },
};
